# Ticker falls back to stderr only when React.reportError itself throws while reporting a tick crash.
art/arcane/react/util/common/scheduling/Ticker.java System.err
art/arcane/react/util/common/scheduling/Ticker.java printStackTrace(
# RingLogHandler renders the throwable into a StringWriter for the web log ring; nothing reaches the console.
art/arcane/react/api/web/RingLogHandler.java printStackTrace(
# The web controller attaches its ring handler to the server logger on purpose.
art/arcane/react/core/controller/WebController.java getServer().getLogger(
# Director and scheduling scaffolding uses printStackTrace as its failure sink before the plugin logger exists.
art/arcane/react/util/common/scheduling/J.java Throwable::printStackTrace
art/arcane/react/util/common/director/DirectorContextHandler.java Throwable::printStackTrace
